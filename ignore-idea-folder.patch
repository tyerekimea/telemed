diff --git a/android/.gitignore b/android/.gitignore
index 48354a3..a08d710 100644
--- a/android/.gitignore
+++ b/android/.gitignore
@@ -99,3 +99,11 @@ app/src/main/assets/public
 app/src/main/assets/capacitor.config.json
 app/src/main/assets/capacitor.plugins.json
 app/src/main/res/xml/config.xml
+
+# Android Studio project-local state (window layout, last-used deploy
+# target, indexing cache, etc.) — regenerates automatically, differs per
+# machine, and newer Android Studio versions add new files here faster
+# than gitignore templates track them. Ignore the whole folder rather
+# than the specific filenames above, which don't cover everything it
+# actually creates.
+.idea/
